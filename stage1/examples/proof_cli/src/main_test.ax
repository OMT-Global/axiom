import "std/env.ax"
import "core/render.ax"

fn proof_mode(): string {
match get_env("__AXIOM_PROOF_CLI_MODE__") {
Some(value) {
return value
}
None {
return "default"
}
}
}

print render_command("status", 3, proof_mode())
print render_command("run", 7, proof_mode())
