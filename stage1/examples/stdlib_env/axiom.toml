[package]
name = "stdlib-env"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[capabilities]
fs = false
net = false
process = false
env = ["__AXIOM_STAGE1_MISSING__"]
clock = false
crypto = false
