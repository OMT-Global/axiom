[package]
name = "conformance-package-visibility-dependency-boundary"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[dependencies]
core = { path = "deps/core" }

[capabilities]
fs = false
net = false
process = false
env = false
clock = false
crypto = false
