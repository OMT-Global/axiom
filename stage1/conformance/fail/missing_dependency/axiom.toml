[package]
name = "conformance-fail-missing-dependency"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[dependencies]
core = { path = "deps/missing" }

[capabilities]
fs = false
net = false
process = false
env = false
clock = false
crypto = false
