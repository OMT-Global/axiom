fn worker_name(): string {
  return "job-worker"
}

fn audit_name(): string {
  return "audit-log"
}

fn build_line(edge: string): string {
  return "session-b: connects " + edge + " to worker and audit"
}

fn ship_line(system: string): string {
  return "shared result: " + system + " is ready"
}
