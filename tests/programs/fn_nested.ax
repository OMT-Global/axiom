fn inner(): int {
  return 2
}

fn outer(): int {
  fn inner(): int {
    return 1
  }

  fn fact(n: int): int {
    if n <= 1 {
      return 1
    }
    return n * fact(n - 1)
  }

  return inner() + fact(4)
}

print outer()
print inner()
